include ./controls
+search(false, files.length)
#play-list.list-container.bg-gradiants
    ul.list-group-flush
        for item in files
            li.list-group-item(id=item.Id tabindex="0")
                span.remove
                    i.mx-1.fas.fa-trash-alt
                span#item-name= item.Name